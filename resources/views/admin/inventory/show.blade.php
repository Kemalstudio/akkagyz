<x-dashboard-layout title="Склад: {{ $product->name }}" active="inventory"><div class="page-head"><div><a class="back-link" href="{{ route('admin.inventory.index') }}">← Склад</a><div class="page-title">{{ $product->name }}</div><div class="page-subtitle">SKU: {{ $product->sku ?: 'не задан' }} · Остаток: {{ $product->stock }}</div></div></div><div style="display:grid;grid-template-columns:1fr 1fr;gap:18px"><form class="card" style="padding:22px" method="POST" action="{{ route('admin.inventory.adjust',$product) }}">@csrf<h3>Корректировка остатка</h3><input class="input" type="number" name="quantity" required placeholder="Например: 10 или -3"><textarea class="input" name="reason" required placeholder="Обязательная причина" style="margin-top:10px;padding:10px"></textarea><button class="btn-accent" style="margin-top:10px">Провести</button></form><form class="card" style="padding:22px" method="POST" action="{{ route('admin.inventory.variants.store',$product) }}">@csrf<h3>Новый вариант</h3><input class="input" name="name" required placeholder="Синий / XL"><input class="input" name="sku" required placeholder="SKU" style="margin-top:8px"><input class="input" name="barcode" placeholder="Штрихкод" style="margin-top:8px"><input class="input" name="options_text" placeholder="Цвет: Синий, Размер: XL" style="margin-top:8px"><div class="toolbar" style="margin-top:8px"><input class="input" type="number" name="price" min="0" placeholder="Цена"><input class="input" type="number" name="stock" min="0" required placeholder="Остаток"></div><button class="btn-accent" style="margin-top:10px">Добавить вариант</button></form></div><section class="card" style="padding:22px;margin-top:18px"><h3>Варианты</h3>@forelse($product->variants as $v)<div style="display:flex;justify-content:space-between;padding:11px 0;border-top:1px solid var(--border)"><div><strong>{{ $v->name }}</strong><div class="entity-meta">{{ $v->sku }} · {{ collect($v->options)->map(fn($x,$k)=>$k.': '.$x)->join(', ') }}</div></div><div class="toolbar"><span>{{ $v->stock }} шт.</span><form method="POST" action="{{ route('admin.inventory.variants.destroy',[$product,$v]) }}">@csrf @method('DELETE')<button class="action-btn danger">Удалить</button></form></div></div>@empty<div class="empty-state">Вариантов нет</div>@endforelse</section><section class="card" style="padding:22px;margin-top:18px"><h3>История движений</h3>@forelse($product->stockMovements->sortByDesc('created_at') as $m)<div style="display:flex;justify-content:space-between;padding:11px 0;border-top:1px solid var(--border)"><div><strong>{{ $m->quantity>0?'+':'' }}{{ $m->quantity }}</strong> · {{ $m->reason }}<div class="entity-meta">{{ $m->user?->name ?: 'Система' }} · {{ $m->created_at->format('d.m.Y H:i') }}</div></div><strong>Баланс: {{ $m->balance_after }}</strong></div>@empty<div class="empty-state">Движений пока нет</div>@endforelse</section><style>@media(max-width:800px){.admin-content>div[style*="grid-template-columns"]{grid-template-columns:1fr!important}}</style></x-dashboard-layout>
