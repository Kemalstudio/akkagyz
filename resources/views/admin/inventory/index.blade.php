<x-dashboard-layout title="Склад" active="inventory"><div class="page-head"><div><div class="page-title">Склад и остатки</div><div class="page-subtitle">Варианты, низкие остатки и движения товаров</div></div><div class="toolbar"><a class="btn-ghost" href="{{ route('admin.inventory.index',['low'=>1]) }}">Низкий остаток</a><a class="btn-accent" href="{{ route('admin.inventory.export') }}">Экспорт CSV</a></div></div><form method="GET" class="search-box" style="margin-bottom:18px"><x-icon name="search" :size="15"/><input name="q" value="{{ request('q') }}" placeholder="Название, SKU или штрихкод"></form><div class="card table-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Товар</th><th>SKU</th><th>Продавец</th><th>Варианты</th><th>Остаток</th><th></th></tr></thead><tbody>@forelse($products as $p)<tr><td><strong>{{ $p->name }}</strong><div class="entity-meta">{{ $p->barcode ?: 'Без штрихкода' }}</div></td><td>{{ $p->sku ?: '—' }}</td><td>{{ $p->seller?->store_name ?: 'Основной магазин' }}</td><td>{{ $p->variants->count() }}</td><td><span class="badge" style="color:{{ $p->stock <= $p->low_stock_threshold?'var(--danger)':'var(--success)' }}">{{ $p->stock }}</span></td><td><a class="action-btn" href="{{ route('admin.inventory.show',$p) }}">Управлять</a></td></tr>@empty<tr><td colspan="6" class="empty-state">Товары не найдены</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $products->links() }}</div></div></x-dashboard-layout>
