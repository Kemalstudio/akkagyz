@php($statusLabels=['pending'=>'Ожидает','processing'=>'В обработке','shipped'=>'В пути','delivered'=>'Доставлен','cancelled'=>'Отменён'])
@php($statusColors=['pending'=>'warning','processing'=>'accent','shipped'=>'accent','delivered'=>'success','cancelled'=>'danger'])
<x-dashboard-layout title="Заказы" active="orders">
<div style="display:flex;justify-content:space-between;align-items:end;gap:16px;margin-bottom:24px;flex-wrap:wrap"><div><div class="page-title">Управление заказами</div><div class="page-subtitle">Контролируйте оплату, обработку и доставку заказов</div></div><form method="GET" class="toolbar"><input class="admin-input" name="search" value="{{ request('search') }}" placeholder="Номер или покупатель"><select class="admin-input" name="status"><option value="">Все статусы</option>@foreach($statusLabels as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select><button class="btn-accent">Найти</button>@if(request()->hasAny(['search','status']))<a class="btn-ghost" href="{{ route('admin.orders') }}">Сбросить</a>@endif</form></div>
<div class="card" style="padding:22px"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Заказ</th><th>Покупатель</th><th>Сумма</th><th>Статус</th><th>Дата</th></tr></thead><tbody>@forelse($orders as $order)<tr><td><a href="{{ route('admin.orders.show',$order) }}" style="font-weight:900;color:var(--accent)">{{ $order->number }}</a><div style="font-size:10px;color:var(--text-faint)">{{ $order->delivery_method==='courier'?'Курьер':'Самовывоз' }} · <a href="{{ route('admin.orders.print',$order) }}" style="color:var(--text-faint);text-decoration:underline">печать</a></div></td><td>{{ $order->user->name }}<div style="font-size:10px;color:var(--text-faint)">{{ $order->city }}</div></td><td style="font-weight:900">{{ number_format($order->total,0,'',' ') }} TMT</td><td><form method="POST" action="{{ route('admin.orders.update',$order) }}">@csrf @method('PATCH')<select name="status" class="admin-input" style="height:34px;color:var(--{{ $statusColors[$order->status]??'accent' }})" onchange="this.form.submit()">@foreach($statusLabels as $value=>$label)<option value="{{ $value }}" @selected($order->status===$value)>{{ $label }}</option>@endforeach</select></form></td><td style="color:var(--text-faint)">{{ $order->created_at->format('d.m.Y H:i') }}</td></tr>@empty<tr><td colspan="5" class="empty-state">Заказы по заданным условиям не найдены</td></tr>@endforelse</tbody></table></div><div style="margin-top:20px">{{ $orders->links() }}</div></div>
</x-dashboard-layout>
