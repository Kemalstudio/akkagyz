<x-dashboard-layout title="Отзывы" active="reviews"><div class="page-head"><div><div class="page-title">Модерация отзывов</div><div class="page-subtitle">Проверенные покупки, жалобы и скрытый контент</div></div><form method="GET"><select class="admin-input" name="status" onchange="this.form.submit()"><option value="">Все статусы</option>@foreach(['published'=>'Опубликованные','hidden'=>'Скрытые','rejected'=>'Отклонённые'] as $v=>$l)<option value="{{ $v }}" @selected(request('status')===$v)>{{ $l }}</option>@endforeach</select></form></div><div class="card table-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Товар</th><th>Автор</th><th>Оценка</th><th>Отзыв</th><th>Жалобы</th><th>Решение</th></tr></thead><tbody>@forelse($reviews as $r)<tr><td>{{ $r->product->name }}</td><td>{{ $r->user->name }}<div class="entity-meta">{{ $r->is_verified_purchase?'Покупка подтверждена':'Не подтверждена' }}</div></td><td>{{ $r->rating }}/5</td><td>{{ Str::limit($r->comment,100) }}</td><td>{{ $r->reports_count }}</td><td><form method="POST" action="{{ route('admin.reviews.moderate',$r) }}">@csrf @method('PATCH')<select class="admin-input" name="status"><option value="published">Опубликовать</option><option value="hidden">Скрыть</option><option value="rejected">Отклонить</option></select><input class="admin-input" name="moderation_reason" placeholder="Причина"><button class="btn-accent">Сохранить</button></form></td></tr>@empty<tr><td colspan="6" class="empty-state">Отзывов нет</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $reviews->links() }}</div></div></x-dashboard-layout>
