<x-dashboard-layout title="Выплаты" active="payouts"><div class="page-head"><div><div class="page-title">Выплаты продавцам</div><div class="page-subtitle">Создание и подтверждение выплат из доступного баланса</div></div></div><form class="card" style="padding:20px;margin-bottom:20px;display:grid;grid-template-columns:2fr 1fr 1fr auto;gap:10px" method="POST" action="{{ route('admin.payouts.store') }}">@csrf<select class="input" name="seller_id" required><option value="">Выберите продавца</option>@foreach($sellers as $s)<option value="{{ $s->id }}">{{ $s->store_name }}</option>@endforeach</select><input class="input" type="number" min="1" name="amount" required placeholder="Сумма"><select class="input" name="method"><option value="bank_transfer">Банк</option><option value="card">Карта</option><option value="cash">Наличные</option></select><button class="btn-accent">Создать</button></form><div class="card table-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Номер</th><th>Продавец</th><th>Сумма</th><th>Статус</th><th>Дата</th><th></th></tr></thead><tbody>@forelse($payouts as $p)<tr><td>{{ $p->number }}</td><td>{{ $p->seller->store_name }}</td><td><strong>{{ number_format($p->amount,0,'',' ') }} {{ $businessSettings->currency }}</strong></td><td>{{ $p->status }}</td><td>{{ $p->created_at->format('d.m.Y') }}</td><td>@if($p->status==='pending')<form method="POST" action="{{ route('admin.payouts.complete',$p) }}">@csrf @method('PATCH')<button class="btn-accent">Оплачено</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="empty-state">Выплат пока нет</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $payouts->links() }}</div></div></x-dashboard-layout>
