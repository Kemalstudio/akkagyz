<x-dashboard-layout title="Платежи" active="payments"><div class="page-head"><div><div class="page-title">Платёжные операции</div><div class="page-subtitle">Единый журнал подтверждений и возвратов</div></div></div><div class="card table-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Дата</th><th>Заказ</th><th>Покупатель</th><th>Провайдер</th><th>Тип</th><th>Статус</th><th>Сумма</th></tr></thead><tbody>@forelse($payments as $p)<tr><td>{{ $p->created_at->format('d.m.Y H:i') }}</td><td><a href="{{ route('admin.orders.show',$p->order) }}">{{ $p->order->number }}</a></td><td>{{ $p->order->user->name }}</td><td>{{ $p->provider }}</td><td>{{ $p->type }}</td><td><span class="badge">{{ $p->status }}</span></td><td><strong>{{ number_format($p->amount,0,'',' ') }} {{ $p->currency }}</strong></td></tr>@empty<tr><td colspan="7" class="empty-state">Платежей пока нет</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $payments->links() }}</div></div></x-dashboard-layout>
