<x-dashboard-layout title="Промокоды" active="promocodes"><div class="page-head"><div><div class="page-title">Промокоды и акции</div><div class="page-subtitle">Скидки, сроки и лимиты использования</div></div></div><form class="card" style="padding:20px;display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:18px" method="POST" action="{{ route('admin.promocodes.store') }}">@csrf<input class="input" name="code" required placeholder="Код"><input class="input" name="name" required placeholder="Название"><select class="input" name="type"><option value="percent">Процент</option><option value="fixed">Сумма</option></select><input class="input" type="number" min="1" name="value" required placeholder="Скидка"><input class="input" type="number" min="0" name="minimum_amount" value="0" placeholder="Мин. заказ"><input class="input" type="number" min="1" name="maximum_discount" placeholder="Макс. скидка"><input class="input" type="number" min="1" name="usage_limit" placeholder="Общий лимит"><input class="input" type="number" min="1" name="per_user_limit" value="1"><input class="input" type="datetime-local" name="starts_at"><input class="input" type="datetime-local" name="expires_at"><label><input type="checkbox" name="is_active" value="1" checked> Активен</label><button class="btn-accent">Создать</button></form><div class="card table-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Код</th><th>Название</th><th>Скидка</th><th>Использовано</th><th>Период</th><th></th></tr></thead><tbody>@forelse($codes as $c)<tr><td><strong>{{ $c->code }}</strong></td><td>{{ $c->name }}</td><td>{{ $c->value }}{{ $c->type==='percent'?'%':' '.$businessSettings->currency }}</td><td>{{ $c->usages_count }} / {{ $c->usage_limit ?: '∞' }}</td><td>{{ $c->starts_at?->format('d.m.Y') ?: 'сейчас' }} — {{ $c->expires_at?->format('d.m.Y') ?: 'без срока' }}</td><td><form method="POST" action="{{ route('admin.promocodes.destroy',$c) }}">@csrf @method('DELETE')<button class="action-btn danger">Удалить</button></form></td></tr>@empty<tr><td colspan="6" class="empty-state">Промокодов нет</td></tr>@endforelse</tbody></table></div></div></x-dashboard-layout>
