<x-dashboard-layout :title="$category ? 'Редактирование категории' : 'Новая категория'" active="categories">
<div class="page-head"><div><a class="back-link" href="{{ route('admin.categories.index') }}">← Категории</a><div class="page-title">{{ $category ? 'Редактирование категории' : 'Новая категория' }}</div><div class="page-subtitle">Название, уровень каталога и порядок отображения</div></div></div>
<form class="card form-card" method="POST" action="{{ $category ? route('admin.categories.update',$category) : route('admin.categories.store') }}">@csrf @if($category)@method('PUT')@endif
<div class="form-grid"><div class="field full"><label>Название</label><input class="input" name="name" required value="{{ old('name',$category->name??'') }}" placeholder="Например, Бумага и блокноты">@error('name')<small>{{ $message }}</small>@enderror</div><div class="field"><label>Родительская категория</label><select class="input" name="parent_id"><option value="">Основная категория</option>@foreach($parents as $parent)<option value="{{ $parent->id }}" @selected(old('parent_id',$category->parent_id??'')==$parent->id)>{{ $parent->name }}</option>@endforeach</select></div><div class="field"><label>Иконка</label><input class="input" name="icon" value="{{ old('icon',$category->icon??'folder') }}" placeholder="folder"></div><div class="field"><label>Порядок</label><input class="input" type="number" min="0" name="sort_order" required value="{{ old('sort_order',$category->sort_order??0) }}"></div></div>
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<div class="form-actions"><a class="btn-ghost" href="{{ route('admin.categories.index') }}">Отмена</a><button class="btn-accent">{{ $category?'Сохранить изменения':'Создать категорию' }}</button></div></form>
</x-dashboard-layout>
