<x-dashboard-layout title="Категории" active="categories">
<div class="page-head"><div><div class="page-title">Категории</div><div class="page-subtitle">Структура каталога и порядок отображения</div></div><div class="toolbar"><form method="GET" class="search-box"><x-icon name="search" :size="16"/><input name="search" value="{{ request('search') }}" placeholder="Найти категорию"></form><a href="{{ route('admin.categories.create') }}" class="btn-accent"><x-icon name="plus" :size="15"/>Новая категория</a></div></div>
@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif
<div class="card table-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Категория</th><th>Родитель</th><th>Товары</th><th>Подкатегории</th><th>Порядок</th><th></th></tr></thead><tbody>@forelse($categories as $category)<tr><td><div class="entity-cell"><span class="entity-icon"><x-icon :name="$category->icon ?: 'folder'" :size="17"/></span><div><div class="entity-name">{{ $category->name }}</div><div class="entity-meta">/{{ $category->slug }}</div></div></div></td><td>{{ $category->parent?->name ?: 'Основная' }}</td><td>{{ $category->products_count }}</td><td>{{ $category->children_count }}</td><td>{{ $category->sort_order }}</td><td><div class="row-actions"><a href="{{ route('admin.categories.attributes.index',$category) }}" class="action-btn"><x-icon name="list" :size="14"/>Характеристики</a><a href="{{ route('admin.categories.edit',$category) }}" class="action-btn"><x-icon name="pen" :size="14"/>Изменить</a><form method="POST" action="{{ route('admin.categories.destroy',$category) }}" onsubmit="return confirm('Удалить категорию?')">@csrf @method('DELETE')<button class="action-btn danger"><x-icon name="trash" :size="14"/></button></form></div></td></tr>@empty<tr><td colspan="6" class="empty-state">Категории не найдены</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $categories->links() }}</div></div>
</x-dashboard-layout>
